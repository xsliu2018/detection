server:
  port: 7001
  servlet:
    session:
      timeout: 120m
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bridge_detection?useSSL=false
    username: root
    password: root0314
mybatis:
  mapper-locations: classpath:mappers/*.xml
  configuration:
    map-underscore-to-camel-case: true
debug: false
logging:
  level:
    top.xsliu.detection.dao: debug


